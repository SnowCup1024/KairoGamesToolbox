# 项目协作入口

本文件随仓库提交，供贡献者和 Agent 使用。只读取本次任务需要的文档章节，不要求每次修改前通读仓库。

## 按任务查阅

- 产品用途、使用方式和目录：查阅 [README.md](README.md)。
- 文件边界、技术约束、验证及发布：查阅 [CONTRIBUTING.md](CONTRIBUTING.md) 的相关章节；共享规则以该文件为准。
- 模组功能汇总：查阅 [Mods/README.md](Mods/README.md)；游戏专属入口和编译依赖只查阅对应游戏目录的 README。

## 工作范围与完成条件

- 遵循当前任务及会话中仍适用的授权，不把历史开发记录当作新任务或持续提交授权。
- 在授权范围内连续完成编辑、必要构建、临时夹具测试和由本次修改引起的修复，无需逐步请求确认。已有结果与当前变更仍相符时复用；检查通过且没有未解决疑点后，不继续扩大探索或验证。
- 只在缺失信息、证据或权限阻塞下一步时询问；等待期间完成不依赖它的工作。无法验证的界面或游戏行为如实列明，不代替用户宣称通过。
- 文档及清理任务只做文件、链接、差异和必要哈希检查，不为验证清理而重新构建缓存。

## 仓库卫生

- 只保留源码、必要资源、测试、可维护的使用说明和长期规范。项目维护文档使用中文，代码标识及命令保持原样，许可证保留上游原文。
- 开发日志、阶段验收清单、一次性计划、审查报告、原始日志和元数据导出不提交，也不作为长期文档的引用目标。临时记录留在任务中；确需落盘时使用忽略的 `.Build`。
- 构建输出统一进入 `.Build`；自动清理的系统临时测试夹具和明确纳入版本管理的自有模组 DLL 是例外，详见贡献规范。
- 清理前区分可重建输出、自有载荷与用户数据；不把商业游戏、存档或未知文件当作垃圾。提交前核对实际暂存文件。

- 开发完成时清理废弃探针、无调用代码、旧测试入口和一次性工具，同步正式文件、类、脚本命名及引用；不得仅注释或排除编译后长期保留。交付清理规则见贡献规范。

- 启动器更新时完整重写根目录 CHANGELOG.md，并随代码提交；只保留最新版本日志，历史通过 Git 查阅。
