<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="KairosoftGameToolbox.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:l="using:KairosoftGameToolbox.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:KairosoftGameToolbox.Views"
    mc:Ignorable="d"
    Title="{l:Text Key='开罗游戏工具箱'}">

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 自绘标题栏：圆角窗口 + Mica 下保持拖拽区；系统最小化/最大化/关闭按钮仍由系统绘制 -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="16,0,0,0">
                <FontIcon Glyph="&#xE7FC;" FontSize="15" Foreground="{ThemeResource AccentBrush}" />
                <TextBlock x:Name="AppTitle" Text="{l:Text Key='开罗游戏工具箱'}" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimaryBrush}" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            OpenPaneLength="190"
            CompactPaneLength="48"
            Background="Transparent" HorizontalContentAlignment="Stretch"
            SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="LibraryNavItem" Content="{l:Text Key='游戏库'}" Tag="library">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{l:Text Key='设置'}" Tag="settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{l:Text Key='关于'}" Tag="about">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid x:Name="PageHost" Background="Transparent">
                <views:LibraryPage x:Name="LibraryPage" />
                <views:SettingsPage x:Name="SettingsPage" Visibility="Collapsed" />
                <views:AboutPage x:Name="AboutPage" Visibility="Collapsed" />
            </Grid>
        </NavigationView>
    </Grid>
</Window>
